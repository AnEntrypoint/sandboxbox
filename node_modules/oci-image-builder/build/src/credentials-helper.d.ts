export declare class DockerCredentialHelpers {
    private dockerConfig;
    constructor(customPathOrConfig?: string | DockerCredsConfig);
    auth(registry: string): Promise<DockerAuthResult>;
    readDockerConfig(customPath?: string): DockerCredsConfig;
    getDockerConfig(): DockerCredsConfig;
    setDockerConfig(config: DockerCredsConfig): void;
}
export declare type DockerAuthResult = {
    Username?: string;
    Secret?: string;
    token?: string;
};
export declare type DockerCredsConfig = {
    credHelpers?: {
        [registryUrl: string]: string;
    };
};
