/**
 * @fileoverview translate docker registry image specifiers like ubuntu:lts to
 * ImageLocation metadata objects
 */
export declare const parse: (specifier: string) => ImageLocation;
export interface ImageLocation {
    protocol: string;
    registry: string;
    namespace?: string;
    image?: string;
    tag?: string;
    digest?: string;
}
