/// <reference types="node" />
import * as fs from 'fs';
import * as walkdir from 'walkdir';
export declare type DirMap = {
    [dir: string]: string;
};
export declare type Options = walkdir.WalkOptions & {
    ignores?: string[];
    ignoreFiles?: string[];
};
export declare const walk: (dir: string, options?: Options | undefined, onStat?: ((path: string, stat: fs.Stats) => void) | undefined) => Promise<string[] | {
    [path: string]: fs.Stats;
}>;
export declare const readIgnore: (file: string) => Promise<string[]>;
