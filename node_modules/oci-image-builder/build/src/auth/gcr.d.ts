import { DockerAuthResult } from '../credentials-helper';
import { ImageLocation } from '../image-specifier';
export declare type GCRAuthOptions = {
    token?: string;
    keyFilename?: string;
    credentials?: {
        private_key: string;
        client_email: string;
    };
};
export declare const handler: (image: ImageLocation, scope: string, options: GCRAuthOptions) => Promise<DockerAuthResult>;
