import { DockerAuthResult } from '../credentials-helper';
import { ImageLocation } from '../image-specifier';
export declare const handler: (image: ImageLocation, scope: string, options?: any) => Promise<DockerAuthResult>;
